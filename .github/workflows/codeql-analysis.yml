#对于大多数项目，这个工作流文件不需要更改；只需n#就可以将其提交到存储库。N#n#您可能希望修改此文件以覆盖分析过的语言集，n#或提供自定义查询或构建逻辑。N#n#*注*n#我们试图检测存储库中的语言。请检查下面定义的`Language‘矩阵，以确认您有正确的n#支持的CodeQL语言集。N#nname：“codeQL”n不：n推：n分支：[master]n拉请求：n#下面的分支必须是n分支之上分支的子集：[主]n调度：n-cron：‘35 9**1’n作业：n分析：n名称：分析n运行：Ubuntu-最新的n操作：n操作：读n安全事件：写入n n策略：n分析快速失败：false n矩阵：n语言：[‘Python’]n#CodeQL支持[‘CPP’，‘cSharp’，‘go’，‘java’，‘javascript’，‘python’]n#了解更多：n#https://docs.github.com/en/free-pro-team@latest/github/finding-security-vulnerabilities-and-errors-in-your-code/configuring-code-scanning#changing-the-languages-that-are-analyzed n步骤：n-name：签出存储库n使用：action/checkout@v2n#初始化用于扫描的CodeQL工具。N-name：initialization CodeQL n使用：gitHub/codeql-action/init@v1n，其中包含：n种语言：${matx.language}n}n#如果希望指定自定义查询，可以在这里或在配置文件中指定。默认情况下，此处列出的查询将覆盖配置文件中指定的任何内容。N#在这里的列表前加上“+”以使用这些查询和配置文件中的查询。N#查询：./path/to/local/query，you-org/you-repo/query@main n n#AutoBuild试图构建任何已编译语言(C/C++、C#或Java)。如果这个步骤失败，那么您应该删除它并手动运行构建(参见下面)n-name：AutoBuildn使用：gitHub/codeql-action/autobuild@v1n#ℹ️命令行程序使用OSshell运行。如果自动编译失败，请删除它并取消注释如下三行(或添加更多代码)，如果项目n#使用编译语言n#运行：n#make https://git.io/JvXDl n#make n#make n-name：PerformCodeQL Analysisn Use：GitHub/codeql-action/Analysis@v1
